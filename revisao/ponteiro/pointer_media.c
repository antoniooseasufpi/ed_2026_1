/*Crie um algoritmo que leia 3 notas de um aluno, atribua a média calculada a um ponteiro para o tipo adequado. Após isso, efetue comparações com o valor da média e informe se o aluno foi aprovado ou reprovado mediante média >=7. No final, o algoritmo deve imprimir a média alocada no ponteiro, usando uma variável normal.*/#include <stdlib.h>#include <stdio.h>int main(){	float n1, n2, n3, media, *p_media;	scanf("%f",&n1);	scanf("%f",&n2);	scanf("%f",&n3);	media = (n1+n2+n3) / 3;	p_media = &media;	if (*p_media >= 7)		printf("Aprovado %.2f\n", media);	else		printf("REprovado %.2f\n", media);	return 0;}